---
status: current
applies_when: 新会话零上下文接手 canonmark——了解背景、当前进度、待办清单与风险后继续推进审读修复与波次 4 前置
not_for: 阶段规划顺序(见 roadmap.md)、验收判定标准(见 acceptance.md)、协议字段定义(见 design/protocol.md)、任务状态台账(见 tasks/README.md)
current_authority: task-current
supersedes: []
superseded_by: []
owner: canonmark
last_reviewed: 2026-07-28
---

# 交接：canonmark 审读修复与波次 4 前置（2026-07-28，目的：续作）

> 本卡写给「只读到这段文字、不记得前面任何内容」的执行者。读完本卡 + 必读文档即可开工。

[Fact] 已确认事实（每条附来源）

- 仓库 `/Users/cicada/SourceCode/canonmark`，当前分支 `feat/p5-anti-rot-p6-reading-path`，HEAD `6e21f78`；`main` 停在 `d81d5e4`。**P5+P6 全部代码只存在于本地分支，`git remote -v` 为空，无任何异地备份**（git 实查，2026-07-28 独立审读复核）。
- 2026-07-28 实测基线（锚定日期的历史记录）：`pytest` 125 passed / 93 subtests；`canon audit docs/ --config canonmark.toml` 六门（V2/V4/V5/V9/V10/V11）全 PASS 退出 0；pre-commit **手动触发** Passed——前提是把 `.venv/bin` 加进 PATH；`.git/hooks/pre-commit` 未安装。
- 阶段状态：P0–P5 完成且独立验收 ACCEPT；P6（`canon read` / `canon index` / `canon mcp` / `canon init --print-mcp`）已实现，首轮独立验收 REJECT（9 问题）→ 修复后复验 ACCEPT（附两项必办，已完成并入 `6e21f78`）。
- A19 对照实验（素材已入库 `tests/fixtures/a19-experiment/`）：四组实测无一答错。标签的价值成立（差别在置信度与是否需要人拍板，不在答案对错）；**`canon_read` 相对「只贴标签」的增量价值未获证明**（夹具太小）。此结论基于每组 n=1，不得在后续文档中引用成既成事实。
- 2026-07-28 第二次独立审读（防实现者自评）用 23 组「回退变异」验证守卫：20 组会红（含安全边界与 fixture 双向 oracle）；**1 组是恒真断言**（见 Risk A⑥）；2 组本无守卫（文档正文数字，已知能力边界）。同时列出 11 处文档失真，**全部未修**。
- 用户已拍板并已执行完毕：提交代码（两个 commit `1ada214`、`6e21f78`）、修复第一次审读的 7 处不符、补齐四项无守卫修复、采纳 skill 分工方向（写作端 `~/.codex/skills/technical-plan-sharding` 管「怎么写」，canonmark 管「机器验」，词汇表以 protocol §4.1 为单一事实源）。
- 用户**未拍板**（不得擅动）：① skill 与协议关于 `background-reference` 映射的冲突裁决；② 是否先推私有远程仓验 CI；③ 公开发布（kickoff 红线）；④ A17/A18 是否由 INSUFFICIENT_EVIDENCE 置 PASS；⑤ `---` 水平线边界的修法取舍；⑥「可机检自述」新功能是否立项。

[Risk] 待决事项与风险（待办全集）

A. 第二次审读的 11 处失真与恒真守卫：**已清零（2026-07-28，随本次提交落盘）**。双视角独立复验均判 ACCEPT——守卫真伪：7 组回退变异逐一亲手复现为红，红的均为独立字面量断言，无残留同源恒真；文档口径：11 处逐条独立实证（含跨仓查 agong commit 667253ff1、读 skill 原文、strict 复现实验、行数实测），无新失真，红线完好。修复过程对审读记录的两点更正：恒真断言实为**两处**同源（INDEX_DESCRIPTION 与宿主话术 MCP_HELP，均由被断言常量拼出），均已改为测试内独立字面量；审读所记「同批 A1/A3 都补了注脚」有误——A3 本无注脚亦无需（其验收项不受破坏性变更影响）。顺带修复同根失真：roadmap P1「默认值 = agong 现值」补时点限定；config.py docstring 对 `v2_path_exceptions` 的过泛措辞精确化。原 11 条清单不再复制，见本卡 git 历史（2026-07-28 早间版）与本次提交 diff。已知遗留（复验如实披露，非恒真缺陷）：INDEX_DESCRIPTION 在保留结尾禁令的前提下于**前部**增写相反指引仍测不出——测试注释已自陈「只能靠人工审阅」。
B. 结构性风险：无 remote 备份（单本地分支）；pre-commit 钩子未安装 + `canon` 不在系统 PATH，「提交自动挡脏文档」回路在当前 checkout 断开（A7 证据栏已如实标注）；Python 3.9/3.10 与 Windows 从未实测；V11 孤儿检测单目录数千文件 O(n²)（一行 dict 缓存可解）。
C. 认知风险：历史验收轮次（波次 2 三轮 REJECT、波次 3 REJECT→ACCEPT、11 组变异等）的**原始输出未入库**，权威文档里只有转述；A19 四组的实际输出只有摘录。引用这些时须注明「转述，不可独立复核」。
D. 发布前阻塞（主 agent 已论证、用户未拍板）：`---` 水平线 bug 正打在 README 的「老项目装上不炸」承诺上（建议修法：看 `---` 后首行像不像 YAML 键值对，仅 gradual 启用，误判方向是降级为提示）；pre-commit 安装收口并从全新 clone 验证；README 权重调整（按 A19 结果，主线应压在「标签+审计」，`canon_read` 作为「更进一步」）。

[Goal] 目标与本次不做

- 本阶段目标：~~清完 Risk A 清单 → 三件套 → 独立复验~~（已完成，见 Risk A 与 [Conclusion]）→ **当前卡点：等用户对决策卡八项逐一裁决**（未拍板六项 + Risk D 中的 pre-commit 收口、README 权重调整）→ 进入波次 4（发布准备，T9）。
- 本次不做（范围外清单，原样保留防扩范围）：公开发布 / 创建任何远程仓（kickoff 红线，用户拍板）；修改 `~/.codex/skills/` 下任何文件（用户全局资产，裁决权在用户）；给 canonmark 写「落地薄 skill」（已论证缓做：等第一个真实使用者接入）；「可机检自述」功能（未立项）；agong_server 侧接入改造。

[Verification] 验收方式与证据格式（从 acceptance.md / kickoff.md 现取）

- 实现者不得自评 PASS；判定五态 PASS / FAIL / BLOCKED / INSUFFICIENT_EVIDENCE / NOT_APPLICABLE；没有证据的 PASS 一律写 INSUFFICIENT_EVIDENCE；重要交付须独立 subagent 验收。
- 验收必须做「回退变异」：把修复改回去看测试是否真的红——本会话两轮靠这招抓出假守卫与四项无守卫修复。测试绿不等于测试有效。
- 每次改动后三件套：`.venv/bin/python -m pytest tests/ -q` 全绿；`.venv/bin/python -m canonmark.cli audit --config canonmark.toml` 全门 PASS 退出 0；`PATH="$PWD/.venv/bin:$PATH" .venv/bin/pre-commit run --all-files` Passed。注意 pytest 只在 `.venv` 里，系统 python3 没有。
- 文档纪律：不写会变的数字（要么命令现取、要么不随增删失真的性质陈述、要么锚定日期的历史记录）；改任何行为必须同步守卫并验证守卫会红；「完成」必须落盘为 git 提交才算。

[Conclusion] 当前结论与进度

波次 1–3 全部完成：六门审计 + 渐进采用 + 防腐烂三检 + 读取三件套均已实现、提交、经多轮独立验收 ACCEPT。可信度收尾亦已完成（2026-07-28）：11 处失真 + 两处恒真守卫全部修复，三件套全绿，双视角独立复验均 ACCEPT，随本次提交落盘。当前卡点只剩**用户拍板**：决策卡八项逐项裁决后进入波次 4。

必读文档（按顺序）：
1. /Users/cicada/SourceCode/canonmark/docs/roadmap.md
2. /Users/cicada/SourceCode/canonmark/docs/tasks/README.md
3. /Users/cicada/SourceCode/canonmark/docs/acceptance.md
4. /Users/cicada/SourceCode/canonmark/docs/design/protocol.md
5. /Users/cicada/SourceCode/canonmark/docs/kickoff.md
6. /Users/cicada/SourceCode/canonmark/docs/progress.md
7. /Users/cicada/SourceCode/canonmark/docs/design/vision.md
8. /Users/cicada/SourceCode/canonmark/tests/fixtures/a19-experiment/README.md
9. /Users/cicada/.codex/skills/technical-plan-sharding/SKILL.md（skill 冲突裁决背景；只读，不得修改）

硬约束：回复用中文（英文术语首现附中文注释）；不谄媚；先分析后动手，放行词＝「同意 / 开干 / 可以 / 按照你的建议来」；教学式输出（结论先行、说人话、真实例子）；实现者不得自评 PASS；kickoff 红线（远程发布、删除类操作、任务书外新功能一律挂起等用户）；canonmark 命名裁决（docs 下禁 agent_ 前缀、kebab-case、ASCII 文件名）。
NEXT_ACTION: 等用户对决策卡八项逐项裁决（① skill 冲突两处 ② 私有远程仓验 CI ③ 公开发布 ④ A17/A18 置 PASS ⑤ `---` 水平线修法 ⑥「可机检自述」立项 ⑦ pre-commit 安装收口 ⑧ README 权重调整）；裁决前不动红线内任何事，裁决后进波次 4（T9）。
