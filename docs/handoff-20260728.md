---
status: current
applies_when: 新会话零上下文接手 canonmark——了解背景、当前进度、待办清单与风险后继续推进（当前:决策卡已执行,待 ⑨ 盲区修法与 ③ 发布时机拍板）
not_for: 阶段规划顺序(见 roadmap.md)、验收判定标准(见 acceptance.md)、协议字段定义(见 design/protocol.md)、任务状态台账(见 tasks/README.md)
current_authority: task-current
supersedes: []
superseded_by: []
owner: canonmark
last_reviewed: 2026-07-29
---

# 交接：canonmark 审读修复与波次 4 前置（2026-07-28，目的：续作）

> 本卡写给「只读到这段文字、不记得前面任何内容」的执行者。读完本卡 + 必读文档即可开工。

[Fact] 已确认事实（每条附来源）

- 仓库 `/Users/cicada/SourceCode/canonmark`，当前分支 `feat/p5-anti-rot-p6-reading-path`，HEAD `64025fa`（`84ed753` 交接卡落盘、`64025fa` 第二次审读修复）；`main` 停在 `d81d5e4`。**全部成果只存在于本地分支，`git remote -v` 为空，无任何异地备份**（git 实查；2026-07-29 零上下文审计复核仍属实）。
- 实测基线（锚定提交的历史记录）：`84ed753` 时点 `pytest` 125 passed / 93 subtests，`64025fa` 新增一条守卫后 126 passed / 93 subtests（2026-07-29 零上下文审计实跑复核；现值以命令现取）；`canon audit docs/ --config canonmark.toml` 六门（V2/V4/V5/V9/V10/V11）全 PASS 退出 0；pre-commit **手动触发** Passed——前提是把 `.venv/bin` 加进 PATH；`.git/hooks/pre-commit` 未安装。
- 阶段状态：P0–P5 完成且独立验收 ACCEPT；P6（`canon read` / `canon index` / `canon mcp` / `canon init --print-mcp`）已实现，首轮独立验收 REJECT（9 问题）→ 修复后复验 ACCEPT（附两项必办，已完成并入 `6e21f78`）。
- A19 对照实验（素材已入库 `tests/fixtures/a19-experiment/`）：四组实测无一答错。标签的价值成立（差别在置信度与是否需要人拍板，不在答案对错）；**`canon_read` 相对「只贴标签」的增量价值未获证明**（夹具太小）。此结论基于每组 n=1，不得在后续文档中引用成既成事实。
- 2026-07-28 第二次独立审读（防实现者自评）用 23 组「回退变异」验证守卫：20 组会红（含安全边界与 fixture 双向 oracle）；**1 组是恒真断言**（见 Risk A⑥）；2 组本无守卫（文档正文数字，已知能力边界）。同时列出 11 处文档失真，**审读时点全部未修**（后已清零，见 Risk A）。
- 用户已拍板并已执行完毕：提交代码（两个 commit `1ada214`、`6e21f78`）、修复第一次审读的 7 处不符、补齐四项无守卫修复、采纳 skill 分工方向（写作端 `~/.codex/skills/technical-plan-sharding` 管「怎么写」，canonmark 管「机器验」，词汇表以 protocol §4.1 为单一事实源）。
- 用户 2026-07-29 对决策卡整体「同意」（＝按建议执行）：①②④⑤⑦⑧ 已执行完毕，③⑥ 按建议搁置；执行中新增一项待拍板（⑨ 审计范围盲区修法）。逐项状态见 Risk D 决策卡（单一清单，不在此重复维护）。

[Risk] 待决事项与风险（待办全集）

A. 第二次审读的 11 处失真与恒真守卫：**已清零（2026-07-28，随本次提交落盘）**。双视角独立复验均判 ACCEPT——守卫真伪：7 组回退变异逐一亲手复现为红，红的均为独立字面量断言，无残留同源恒真；文档口径：11 处逐条独立实证（含跨仓查 agong commit 667253ff1、读 skill 原文、strict 复现实验、行数实测），无新失真，红线完好。修复过程对审读记录的两点更正：恒真断言实为**两处**同源（INDEX_DESCRIPTION 与宿主话术 MCP_HELP，均由被断言常量拼出），均已改为测试内独立字面量；审读所记「同批 A1/A3 都补了注脚」有误——A3 本无注脚亦无需（其验收项不受破坏性变更影响）。顺带修复同根失真：roadmap P1「默认值 = agong 现值」补时点限定；config.py docstring 对 `v2_path_exceptions` 的过泛措辞精确化。原 11 条清单不再复制，见本卡 git 历史（2026-07-28 早间版）与本次提交 diff。已知遗留（复验如实披露，非恒真缺陷）：INDEX_DESCRIPTION 在保留结尾禁令的前提下于**前部**增写相反指引仍测不出——测试注释已自陈「只能靠人工审阅」。另一处如实标注：config.py 对 `v2_path_exceptions` 的词级措辞精确化落在复验判定之后、提交之前，该半句未被复验覆盖（纯注释措辞，风险极低）。
B. 结构性风险（2026-07-29 更新）：备份已解——私有仓 `deanjo/canonmark` 建立，feat 与 main 双分支已推，PR#1 首跑 CI 两 job pass；pre-commit 回路已闭合——钩子装入本仓与全新 clone，双向实测（坏 frontmatter 提交被拦 exit 1、干净提交放行），PATH 前提保留（提交环境需把 venv 的 bin 加进 PATH）。仍在：Python 3.9/3.10 与 Windows 从未实测；V11 孤儿检测单目录数千文件 O(n²)（一行 dict 缓存可解）；CLI 不自动发现 cwd 的 `canonmark.toml`，须显式 `--config`（既有设计——hook 与 CI 均显式传参，徒手跑时拿到的是默认 gradual 而非仓库配置，易踩）。
C. 认知风险：历史验收轮次（波次 2 三轮 REJECT、波次 3 REJECT→ACCEPT、11 组变异等）的**原始输出未入库**，权威文档里只有转述；A19 四组的实际输出只有摘录。引用这些时须注明「转述，不可独立复核」。2026-07-28 双视角复验与 2026-07-29 零上下文审计的报告同此模式（原始输出未入库，仅转述进本卡与 progress）；其中「两项必办已并入 `6e21f78`」在盘上仅能溯源到本卡自身，无第二来源可交叉验证。
D. 决策卡（2026-07-29 用户「同意」＝按建议执行；逐项现状）：
  ① skill×协议映射冲突——**已执行**：采纳协议立场，skill §1.5 已改为引用 protocol §4.1（附裁决日期注记，可一眼回退）；协议 §4.1 记录裁决收口，历史冲突记录保留。
  ② 私有远程仓验 CI——**已执行**：私有仓 `deanjo/canonmark`，双分支已推，PR#1 首跑 CI（tests / self-audit）双 pass——项目首次有异地备份与真实 CI。
  ③ 公开发布——**搁置中**（建议：发布物收口后由用户定时机）；红线不变。
  ④ A17/A18 置 PASS——**已执行**：状态置 PASS，acceptance/roadmap/tasks/README/本卡五处口径同步。
  ⑤ `---` 水平线修法——**已执行**：gradual 下看 `---` 后首个非空行像不像 YAML 键值对，不像即视为未治理（提示，exit 0）；strict 不变；缺闭合照旧报错；回退变异 3 条测试红；独立复验判定一 ACCEPT（10 组边界探针）；两条已知代价（注释行开头的标签失效、`status:current` 手误静默放过）记入 acceptance「未验证范围」。
  ⑥ 「可机检自述」立项——**搁置中**（建议：等第一个真实使用者暴露需求）。
  ⑦ pre-commit 安装收口——**已执行**：本仓 + 全新 clone 双向实测，PATH 前提写入 A7；实测顺带发现 ⑨ 的盲区。
  ⑧ README 权重调整——**已执行**：主线压「标签+审计」（A19 已证价值），`canon_read` 降为「更进一步：执行层」。
  ⑨ **新增待拍板**：审计范围盲区修法——非 key 目录（docs 根及默认 `design`/`tasks`/`gates` 之外）、无权威信号的文档零校验，`status: bogus-value` 在任何模式下静默全 PASS（2026-07-29 钩子实测发现，独立复验修正范围后坐实；详见 acceptance「未验证范围」）。建议修法：任何带 frontmatter 的文档无论位置，至少校验 status 枚举合法性与 superseded 指针一致性。

[Goal] 目标与本次不做

- 本阶段目标：~~清完 Risk A 清单 → 三件套 → 独立复验~~ ~~决策卡执行~~（均已完成，见 Risk A/D 与 [Conclusion]）→ **当前待用户：⑨ 盲区修法是否立项、③ 公开发布时机** → 波次 4（T9 发布准备）随 ③ 启动。
- 本次不做（范围外清单，原样保留防扩范围）：公开发布 / 创建任何远程仓（kickoff 红线，用户拍板）；修改 `~/.codex/skills/` 下任何文件（用户全局资产，裁决权在用户）；给 canonmark 写「落地薄 skill」（已论证缓做：等第一个真实使用者接入）；「可机检自述」功能（未立项）；agong_server 侧接入改造。

[Verification] 验收方式与证据格式（从 acceptance.md / kickoff.md 现取）

- 实现者不得自评 PASS；判定五态 PASS / FAIL / BLOCKED / INSUFFICIENT_EVIDENCE / NOT_APPLICABLE；没有证据的 PASS 一律写 INSUFFICIENT_EVIDENCE；重要交付须独立 subagent 验收。
- 验收必须做「回退变异」：把修复改回去看测试是否真的红——本会话两轮靠这招抓出假守卫与四项无守卫修复。测试绿不等于测试有效。
- 每次改动后三件套：`.venv/bin/python -m pytest tests/ -q` 全绿；`.venv/bin/python -m canonmark.cli audit --config canonmark.toml` 全门 PASS 退出 0；`PATH="$PWD/.venv/bin:$PATH" .venv/bin/pre-commit run --all-files` Passed。注意 pytest 只在 `.venv` 里，系统 python3 没有。
- 文档纪律：不写会变的数字（要么命令现取、要么不随增删失真的性质陈述、要么锚定日期的历史记录）；改任何行为必须同步守卫并验证守卫会红；「完成」必须落盘为 git 提交才算。

[Conclusion] 当前结论与进度

波次 1–3 全部完成；可信度收尾完成（2026-07-28，11 处失真 + 两处恒真守卫修复，双视角复验 ACCEPT）；决策卡执行完成（2026-07-29）：六项落地（水平线修复、私有仓 + 首次 CI 双绿、pre-commit 回路闭合、skill 冲突裁决收口、A17/A18 置 PASS、README 权重调整），③⑥ 按建议搁置。当前待用户两件事：⑨ 盲区修法拍板、③ 发布时机。波次 4（T9）随 ③ 启动。

必读文档（按顺序）：
1. /Users/cicada/SourceCode/canonmark/docs/roadmap.md
2. /Users/cicada/SourceCode/canonmark/docs/tasks/README.md
3. /Users/cicada/SourceCode/canonmark/docs/acceptance.md
4. /Users/cicada/SourceCode/canonmark/docs/design/protocol.md
5. /Users/cicada/SourceCode/canonmark/docs/kickoff.md
6. /Users/cicada/SourceCode/canonmark/docs/progress.md
7. /Users/cicada/SourceCode/canonmark/docs/design/vision.md
8. /Users/cicada/SourceCode/canonmark/tests/fixtures/a19-experiment/README.md
9. /Users/cicada/.codex/skills/technical-plan-sharding/SKILL.md（skill 冲突裁决背景；只读，不得修改）

硬约束：回复用中文（英文术语首现附中文注释）；不谄媚；先分析后动手，放行词＝「同意 / 开干 / 可以 / 按照你的建议来」；教学式输出（结论先行、说人话、真实例子）；实现者不得自评 PASS；kickoff 红线（远程发布、删除类操作、任务书外新功能一律挂起等用户）；canonmark 命名裁决（docs 下禁 agent_ 前缀、kebab-case、ASCII 文件名）。
NEXT_ACTION: 等用户两件事——⑨ 审计范围盲区修法是否立项（见 Risk D ⑨ 与 acceptance「未验证范围」）、③ 公开发布时机；其余六项已于 2026-07-29 执行完毕并随本次提交落盘。红线不变。
